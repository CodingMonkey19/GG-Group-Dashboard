#!/usr/bin/env bash
# Start the GG Spend Dashboard backend (Fastify on PORT or 8080, LAN-bound).
# In production, the backend also serves frontend/dist as static assets.
set -euo pipefail
cd "$(dirname "$0")/.."
exec npx --prefix backend tsx backend/src/api/server.ts "$@"
